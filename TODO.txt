(empty)

The four items that were here — back-button losing query params, pages numbered from zero, filter
links dropping the filter, and the responsive collapsing menu — are all done. See the "Paged,
sorted and filtered listings" and "Frontend routing/auth" sections of CLAUDE.md for what the fixes
were and why each of them was a bug.
